/**\n * DSH Model Search Plugin — Client Type declarations\n */\n\n/**\n * Configuration options for the model search plugin.\n */\nexport interface ModelSearchPluginOptions {\n  placeholder?: string;\n  debounceDelay?: number;\n  hideUnmatched?: boolean;\n  focusHotkey?: string;\n  focusRequiresModifier?: boolean;\n  minQueryLength?: number;\n  containerClass?: string;\n  inputClass?: string;\n  showNoResults?: boolean;\n}\n\n/**\n * Configure the plugin before activation.\n */\nexport function configure(options?: ModelSearchPluginOptions): void;\n\n/**\n * Activate the plugin: inject styles + start observing.\n */\nexport function activate(): void;\n\n/**\n * Deactivate the plugin: remove all injected DOM and observers.\n */\nexport function deactivate(): void;\n\n/**\n * DSH client plugin entry point — called by the Cordis Loader.\n */\nexport function apply(ctx: any, options?: ModelSearchPluginOptions): void;\n