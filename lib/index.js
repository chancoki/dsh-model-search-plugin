/**
 * DSH Model Search Plugin — host-side stub
 *
 * The search logic lives in the browser half (./client.js).
 * This stub satisfies the host-side Cordis entry requirement.
 */
export function apply(ctx, config) {
  // Host-side: no runtime behaviour needed.
}